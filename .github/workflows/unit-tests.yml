# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Unit Tests

on:
  push:
    branches: [ "main" ]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    name: test (Spark ${{ matrix.spark-version }})
    strategy:
      matrix:
        spark-version: ["4.0.0", "4.1.0"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build and verify the local deployment.
      run: build/sbt -DsparkVersion=${{ matrix.spark-version }} clean package publishM2

    - name: Setup pinned Delta for testing
      env:
        DELTA_SPARK_VERSION: ${{ matrix.spark-version }}
      run: bash project/scripts/setup_delta_atomic_rtas.sh

      # 'integrationTests' is not in root project, since we avoid running it via `./build/sbt test`,
      # Here, we explicitly compile `integrationTests` to catch compilation issues early.
    - name: Compile integration tests to verify compilation and checkstyle issues.
      run: build/sbt -DsparkVersion=${{ matrix.spark-version }} -DdeltaVersion=4.2.0-SNAPSHOT integrationTests/test:compile

    - name: Run tests with coverage
      run: build/sbt -DsparkVersion=${{ matrix.spark-version }} -DdeltaVersion=4.2.0-SNAPSHOT -J-Xmx2G +jacoco

    - name: Run license check
      run: build/sbt -DsparkVersion=${{ matrix.spark-version }} -DdeltaVersion=4.2.0-SNAPSHOT licenseCheck
